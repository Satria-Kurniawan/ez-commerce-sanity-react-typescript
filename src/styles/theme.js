import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9c27b0",
      light: "#9c27b0",
      dark: "#9c27b0",
    },
    secondary: {
      main: "#F2DEFF",
      light: "#F2DEFF",
      dark: "#F2DEFF",
    },
    common: {
      white: "#fff",
      black: "#000",
    },
  },
});

export default theme;
